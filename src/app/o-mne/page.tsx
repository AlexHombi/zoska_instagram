// src/app/o-mne/page.tsx

import Typography from "@mui/material/Typography";

export const metadata = { title: `o-mne | zoska-instagram` };

export default function AboutMe() {
  
  return (

      <Typography> o-mne </Typography>

  );
}